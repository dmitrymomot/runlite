{
	# Enable on-demand TLS with domain registry verification
	on_demand_tls {
		ask http://127.0.0.1:3000/domains/{host}/verify
		interval 2m
		burst 5
	}
}

# All routes are added dynamically via Caddy Admin API
# See internal/caddy/client.go for route management
